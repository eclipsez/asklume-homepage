# 问答光源｜AskLume 网站设计基准

## 1. 唯一正式版本

- `src/App.tsx` 是首页唯一入口。
- `src/resources/ResourcesPage.tsx` 是资源中心入口。
- `src/about/AboutPage.tsx` 是关于我们入口。
- 正式代码中不使用版本编号、版本切换器或版本参数。
- `archive/v1/` 仅用于保存旧版，不参与构建、测试或后续修改。

## 2. 品牌与产品命名

- 中文与国内场景：`问答光源｜AskLume`
- 海外品牌：`AskLume`
- 技术系统：`GEO-AIP™`
- 方法论：`数字证据工程`
- 服务入口：`AI认知基线诊断`

不得使用 `Ask Lume`、`Asklume`、`ASKLUME` 或 `AskLume AI` 作为默认品牌名。

## 3. 页面结构

### 正式首页

`Header → Hero → 核心优势 → GEO-AIP™ 三大支柱 → 能力体系 → 诊断入口 → Footer`

### 资源中心

`Header → 资源 Hero → 推荐阅读 → 检索与筛选 → 服务路径 → 边界 FAQ → 诊断入口 → Footer`

### 关于我们

`Header → 品牌使命 → 我们做与不做 → 工作方法 → 品牌原则 → 协作方式 → 初筛入口 → Footer`

Header、Footer、移动菜单和导航数据必须全站复用，不在二级页面单独复制。

## 4. 代码边界

- 全站导航与联系方式：`src/content/siteContent.ts`
- 全站设计变量：`src/styles/tokens.css`
- 正式共享组件：`src/components/`
- 页面级内容与交互：`src/resources/`
- 关于我们页面：`src/about/`
- 旧版归档：`archive/v1/`

新增页面必须使用正式 Header、Footer 和全局 token。禁止从 `archive/` 导入代码。

## 5. 视觉规则

- 页面宽度：最大 `1200px`
- 页面背景：冷白浅灰
- 品牌强调色：蓝、紫、洋红组成的 AskLume 光谱
- 卡片圆角：`24px`
- 控件圆角：`16px`
- 主按钮：全圆角
- 阴影：使用带紫色倾向的柔和阴影，禁止纯黑重阴影
- 导航高度：桌面 `76px`，移动端 `64px`
- 页面主题：全页保持同一浅色主题，不在单个区块反转

设计变量必须通过语义 token 调用，不在组件中新增 `v1`、`v2` 或其他版本前缀。

## 6. 内容与转化规则

标准服务路径：

`免费初筛 → 付费基线诊断 → 根据诊断结果决定建设 → 持续监测`

- 不承诺 AI 排名、首位推荐、固定推荐率或营收结果。
- 可承诺的范围是诊断、证据结构、内容资产、技术可访问性、平台采样和持续复测。
- 示例指标必须明确标注为示例数据。
- 一个页面对同一转化意图使用同一个 CTA 名称。

## 7. 响应式与可访问性

- 多列布局必须在 `768px` 以下明确切换为单列。
- 禁止横向页面溢出。
- 移动菜单必须支持 Escape 关闭、焦点锁定和关闭后焦点恢复。
- 动画必须支持 `prefers-reduced-motion`。
- 表单必须有可见标签，按钮和文字必须满足 WCAG AA 对比度。

## 8. 每次修改的工作方式

1. 明确修改页面和组件名称。
2. 先检查 `siteContent.ts` 与 `tokens.css`，避免复制内容或颜色。
3. 只修改正式目录，不修改归档。
4. 执行 `npm run build` 和 `npm test -- --run`。
5. 在桌面端和 `390px` 移动端检查首屏、导航、锚点和横向溢出。

今后的任务应使用“正式首页 Hero”“全站 Header”“资源中心筛选”等名称，不再使用版本编号描述页面。
